num = float(input('Qual é o salário de um funcionário? R$'))

porcentagem = num * 1.15

print('Um funcionário que ganhava R${:.2f}, com 15% de aumento, passa a receber R${:.2f}'.format(num, porcentagem))
